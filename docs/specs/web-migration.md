# Web 前端与通信迁移规格

> 当前目录修订：根目录 `backend/` 已移除，原对话适配、日志读取和界面资源存储分别迁至 `webapp/chat_service.py`、`webapp/console_log.py`、`webapp/ui_store.py`；前端交互的服务端代码统一归 `webapp/`。下文旧目录属于迁移阶段记录，以本修订和宏观架构最新修订为准。

> 本文记录展示与通信迁移阶段。v0.01 已将对话空接口替换为 Python Agent loop；请求新增 `run_id`，新增运行查询、停止和服务端历史，具体以 [Agent loop v0.01](agent-loop-v0.01.md) 为准。下文 501 与空实现描述仅对应迁移阶段；未迁移的其他业务仍返回 501。


更新：2026-09-14。用户纠正上一版的裁剪范围：**原对话框和已经写好的设置功能全部保留，对话接口放到根目录 backend，暂不实现对话业务逻辑。**

## 当前结构

- `webfrontend/src/gwc/`：恢复的完整原界面、资源管理、所有设置页签、立绘与剧本编辑器。原 JSX 结构和 UI 处理保留；主对话、记忆整理、剧情推演和剧本生成中的旧模型执行已移除。
- `webfrontend/src/App.tsx`：挂载完整原界面的入口，取代上一轮的简化标题、对话和视觉设置页面。
- `webfrontend/src/services/`：新增 TypeScript 对话客户端与同源请求适配。后端 501 不能被原设置组件当作保存或执行成功。
- `backend/chat.py`：对话输入模型、应用调用接口与空实现，不依赖 FastAPI，不执行 LLM、上下文、记忆、工具或会话编排。
- `backend/ui_store.py`：开发预览的设置、界面存档与媒体文件访问，按浏览器 Cookie 分目录；不是长期业务 Session 或 MySQL 持久化实现。
- `webapp/`：FastAPI 组装、对话适配、设置/资源通信路由和静态资源读取。
- `webfrontend/legacy/`：保留的原始迁移参考，不进入新入口或静态发布。与 `src/gwc/` 不同，这里保持上游原版以便对照。
- `legacy/gwcpro/`：从根目录 `gwcpro/` 整体迁入，旧后端模型及其他业务保持原样；已迁出的展示入口仍保留迁移注释。

新增 Python 和 TypeScript 有类型及中文文档。恢复的上游 JSX/JS 按触及范围改造，未做无关全量重写；新增 TS 严格检查，恢复的 JS 检查语法与未定义标识符，第三方打包库不纳入业务 lint。

## 对话行为

原来的 START、CONTINUE、对话输入框、附件、快捷栏、历史面板及存读档面板均保留。输入和发送可以操作，发送调用 `POST /api/chat/messages`，再由 `webapp/chat.py` 调用 `backend/chat.py`。

请求只包含正文和附件，不包含前端拼装的模型历史、system prompt 或 API Key。当前空实现返回 HTTP 501 与 `chat_not_connected`，前端显示后端提示、恢复按钮状态，保留草稿和附件；不伪造 assistant 回复，也不保存为已经完成的对话。

用户保留了旧界面中的存档和历史入口，但这不代表 Tidebound 的业务 Session、管理员回退或停止协议已经实现。没有模型执行，原前端桥接、桌宠和执行结果轮询不启动。

## 设置与资源

保留视觉设定、文本互动、声音设定、剧本角色、模型接口、数据管理、插件模组、技能控制台、知识库、账号安全、登录定制、关于系统，以及原 BOT、立绘与剧本编辑器入口。

外观、文本参数、快捷键、角色配置、音频/背景/模型上传、登录外观与插件数据继续通过原控件操作。预览设置和资源保存于 `data/ui-preview/<浏览器标识>/`，刷新后可读。这个目录不进入版本控制，不读取旧账号数据。

HTTP Cookie 是开发预览的数据范围标识，不代表登录或管理员认证。客户端不能通过旧 mirror_id 路径读取其他浏览器数据；拒绝路径越界及跨来源写入，开发 Vite 来源单独允许。文件原子写入，不解压或执行上传内容。

账号管理、技能执行、知识库构建、模型安装、QQ Bot 等依赖尚未迁移后端的入口和配置仍保留，但后台动作返回未接入。这些功能的保留是源代码与界面的保留，不伪造相应服务已经运行。导出当前预览备份只读取本浏览器，不枚举旧用户。

## 启动与验证

依旧使用 `uv run uvicorn webapp.main:app --host 127.0.0.1 --port 5201`；前端在 `webfrontend` 执行 `npm run dev`，或构建后访问 `/app/`。对话应用接口放在 backend，FastAPI 启动入口仍是 webapp，避免重复通信层。

验收覆盖全部原设置页签、实际对话请求与 501、设置刷新恢复、文件上传和备份导出、开发代理、Cookie 隔离与资源边界。仓库未提供完整角色包，因此真实 Live2D 纹理与动作仍需角色包后验收；本轮在 macOS 测试，未宣称三平台或生产环境已经验证。

## 本轮验证结果

- Python：26 项通过，含对话空接口校验、不执行/不保存、设置持久化、Cookie 隔离、中文资源上传、模型相对路径、插件资源、路径越界及来源校验。
- 浏览器：5 项通过，覆盖原完整界面与全部 12 个设置页签、发送失败保留草稿、Vite 开发模式、设置刷新恢复、背景上传与 ZIP 备份导出。
- TypeScript 构建检查、恢复 JS 的语法/未定义标识符检查、Python lint 均通过。
- 仍有第三方 Starlette/AnyIO 的 2 条弃用提示，以及恢复完整前端后 Vite 的单包体积提示；没有把这些提示隐藏为已修复。

## 2026-09-14 账号与日志入口

用户入口改为 `/app/{uid}`；MySQL 账号鉴权与管理员 `/app/{uid}/console` 已接入，覆盖此前预览 Cookie 和未接入账号的描述。Console 当前只读取固定 `data/agent-debug.log`。详见 [账号鉴权与日志 Console](accounts-and-console.md)。


## 2026-09-16 请求体接收中断

读取请求体期间的 `ClientDisconnect` 由通信层按 499 请求中断处理，不上报为未处理的 ASGI 服务异常，也不返回保存成功。界面 JSON 配置仍须完整接收和解析后才原子写入，未接收完整的保存请求不得覆盖原配置。此处理不停止已开始的 Agent 执行。

## 2026-09-18 上游前端增量同步

本次比较基线为 `b1d31f5aea49324f23470c4d6c07c3e918de7080`（与 `webfrontend/legacy/frontend/src/` 的原始树一致），目标为 GWC-Pro `main` 的 `59428e5d498adbef826d08cb4c6321864fd5c327`。该区间的 `frontend/` 仅修改 `src/AppCore.jsx`、`src/components/settings/SoundTab.jsx` 和 `src/utils/constants.js`，分别适配至 `webfrontend/src/gwc/`；没有依赖或静态资源增量。`legacy/` 仍保留首次迁移参考，下次增量同步应以上述目标提交为比较起点，同时保留本文记录的排除项。

Git 历史通过双父提交的选择性合并保留上述上游原始提交，作者为 QiuSui1145，原哈希和时间不变；合并提交记录 Tidebound 的路径映射、兼容适配及验证。上游提交中的非前端改动明确排除，因此该合并关系不表示其后端功能已经迁入。

- 保留 GPT-SoVITS / Qwen3-TTS 引擎选择、音色编辑、模型与 PyTorch 管理、硬件检测及配置文档界面，新增配置使用现有界面存储持久化。
- 保留上游音频队列等待加载、文本分句及情绪标签解析代码；不向 Tidebound 聊天流程新增配音调用。实际语音生成、模型安装与硬件检测仍未接入，也未验证真实音频播放。
- 所有新增管理请求沿用 `frontendFetch` 的同源鉴权和错误处理，未迁移服务继续返回 501。面板明确展示未接入提示，状态查询失败显示“服务不可用”，不推断为未安装。内嵌安装文档标注为上游参考。
- 排除上游 AppCore 中属于旧模型执行流程的情绪提示词注入、503/429 自动重试及旧回复提交处的标签清理；保留当前 Python Runtime、停止、历史、流式回复及欢迎工作流适配。

验证范围为前端 lint、TypeScript 与生产构建，以及展示/聊天和欢迎流程浏览器回归。双引擎回归覆盖默认 GPT-SoVITS、Qwen3 切换与刷新恢复、501 可见提示及上游文档标记；不声明已实现 TTS 后端能力。

本轮结果：lint、构建和 19 项 Chrome 浏览器用例通过；Vite 仍提示单包超过 500 kB。测试断言完成后两个 Playwright worker 在退出阶段未响应 SIGTERM，清理本轮 worker 后测试主进程以 0 退出并报告 `19 passed`。
