# Dev 云端情感增强

日期：2026-09-20。用户要求将已验证的云端 instruct 模型作为回复润色步骤；保留其亚托莉身份，只轻调 system，让小模型只完成润色。按最新确认，入口从 Dev 工具箱移至聊天底部菜单栏，作为独立功能。

后续安全护栏与原稿保真校验由 [issue #14](https://github.com/Kevinn6366/Tidebound/issues/14) 跟踪。当前协议检查不等于语义保真或完整安全验收，以下真实样本限制仍有效。

## 使用范围与开关

聊天底部菜单栏增加独立的“情感增强 · 开/关”按钮，点击直接切换，配色与其他菜单按钮一致，通过文字标明开关状态，Dev 工具箱不再提供此项。当前仍限 dev 管理员使用，默认关闭。开关只作用于当前账号，从下一次 Run 生效；保存到 `data/agent/<scope>/state/emotion-enhancement.json`，服务重启后保留，清空对话不清除开关。当前 Run 固定开始时的开关、独立渠道和提示词内容，不受运行中切换影响。

`GET/PUT /api/chat/emotion-enhancement` 要求登录管理员及 dev 模式，目标固定为本人内部 scope。PUT 只接受严格布尔 `enabled`，不接受 scope、模型、提示词、地址或 Key。响应为 `enabled/configured/model`；configured 只表示后端配置齐备，不等于刚刚完成联网验证。未配置时不能开启，仍可关闭已有开关。

## 一次润色的材料

每条准备显示的完整回复调用一次 instruct，不按标点拆分句子。搜索首段和正式续答是两段分别完成的回复，各自润色一次；最终合并时不重复润色。不是新的自主 Agent，也不增加小模型多轮会话。

请求只有两条消息：

1. 一条固定 system：保留桌面验证版中的亚托莉身份、情绪、自我意识、水菜萌等人物背景与自然表达方式，仅调整为润色用途。
2. 一条 user：唯一的一条润色指令加待说出口的原稿正文，原稿属于亚托莉自己，不是新的用户问题。

user 只提供一条润色任务：保持原意、事实、称呼、数字、链接和不确定性，仅轻微调整语气和措辞，只返回正文，不回答或续写。system 明确原稿说话归属和最小修改边界；真实调试发现直接把原稿作为用户台词会引起角色指代反转，因此用单条明确任务区分原稿和对方的话。

2026-09-20 用户认为问候原稿优于增强结果后，system 保留亚托莉及水菜萌等核心身份背景，缩短自由扮演说明，移除“避免每轮追问”等可能与保留原稿冲突的泛化要求。校对与保真规则置于首位，仅修正明确错字或语病，通顺自然时逐字原样输出。保留每句话、问题、称呼、身份描述与段落；身份背景不能被扩写成“能感受情绪”等台词，也不据此新增经历、动作或承诺。仍只有原有一条 user 润色任务，不增加评审模型或自动重试。

不发送完整历史、主模型 system、工具声明、工具结果或 reasoning 字段；不要求解释、自评或再次修改。小模型只调用一次，无自动重试、无多候选选择。提示词统一在 `prompts/master/emotion.enhancement/`，角色和唯一任务分别通过 manifest purpose `emotion.enhancement` / `emotion.enhancement.instruction` 加载；不更改主 Agent 的角色提示词。

## 出口与展示

开启后处理普通聊天最终回复、登录问候、搜索第一反应及搜索正式续答。摘要、跟进摘要、搜索语境和阅读印象等内部节点保持原状；搜索的隐藏草稿不进行额外润色。

原稿生成及润色期间关闭页面正文预览。工具前过渡文字直接隐藏。润色模型本身使用非流式请求，完成后沿既有页面打字展示机制交付；不承诺开启后仍有主模型的逐 token 首字延迟。搜索首反应保留原有时间和长度上限，成功润色并验证后才写入 `first_reaction` 或发布预览。

正式 Run 消息、页面历史以及下一轮上下文使用润色后的正文；工具调用、工具结果与原模型 reasoning 保持原协议。历史恢复或回放不重新调用润色模型。

## 失败、停止与审计

普通回复及欢迎润色失败、超时、空正文、截断、非 assistant 或携带工具调用时，本轮失败，不用原稿冒充增强成功。搜索首反应沿既有失败策略撤回首段并继续正式搜索，不展示失败原稿。

润色处于原有 Run 超时和取消范围内。停止、清空或时间线改变后，迟到结果不能展示或提交；提交前仍检查停止及时间线资格，不在最后检查与存储之间新增等待。

润色使用独立预算检查，不计为主 Agent 的工具推理轮次。Console 请求快照以 `emotion.enhancement` purpose 关联相同账号、Run 和调用阶段，记录固定 system 与单条“指令 + 原稿”，不含鉴权头或 Key。主请求预算圆环仍表示主模型上下文，不将二次调用伪装成同一个模型的精确 token 用量。

## 服务端配置

独立使用以下环境变量，不回退到主模型或中转凭据：

| 变量 | 含义 |
| --- | --- |
| `TIDEBOUND_EMOTION_BASE_URL` | OpenAI 兼容基础地址，到 `/v1` |
| `TIDEBOUND_EMOTION_MODEL` | 部署后的模型 Code |
| `TIDEBOUND_EMOTION_API_KEY` | 与该云端服务匹配的 Key |
| `TIDEBOUND_EMOTION_TIMEOUT` | 单次润色最大等待，默认 30 秒；同时受 Run 剩余时间限制 |
| `TIDEBOUND_EMOTION_MAX_OUTPUT_TOKENS` | 默认预留 2048 输出 tokens |
| `TIDEBOUND_EMOTION_CONTEXT_LIMIT` | 默认 32768 总预算，仍按项目保守字节口径核算 |

部署实际支持范围由提供方确认，预算值并非自动探测结果。配置修改需重启后端，提示词内容在下一次 Run 开始时重新加载。真实 Key 只进入被忽略的本地 `.env`，不进入版本控制或前端。

## 验收项目

- 默认关闭保留原有模型调用与流式行为。
- 每个对用户展示的回复出口只发起一次润色；内部节点和工具调用不润色。
- 原稿不提前出现在预览、首反应或正式页面历史中，展示与有效上下文正文一致。
- 请求材料仅固定 system、单条原稿，无历史、工具、reasoning 或额外指令。
- 账号隔离、重启和清空后的开关持久化、执行中切换后的快照稳定。
- 润色失败和迟到结果不能绕过停止、清空或提交检查。
- 用现有云端模型完成一条独立真实润色测试，并分别记录程序验证与表达效果。

## 2026-09-20 验证结果与表达限制

Python 全量 209 项通过；唯一润色指令定稿后，增强相关 24 项再次通过。Ruff、前端类型检查、lint、生产构建及迁移入口前的 3 项 Chrome 工具箱测试通过。

底部菜单入口迁移后，前端类型检查、lint、生产构建和更新后的 3 项 Chrome 界面测试通过，覆盖保存等待、失败保留、刷新恢复、未配置可关闭和加载重试。已在真实服务的 Chrome 测试页确认开关可正常关闭、开启，并保持开启供继续对话。

使用本机已配置的 instruct 服务，通过真实 `ChatSession` 完成固定原稿 → 云端润色 → 保存回复的验证，增强请求计数为 1。主模型原稿使用受控测试句，此次不冒充两个真实模型的联合推理评估。

合成原稿：`现在是 21:30，你明天 8 点还有课，先去睡吧。`

实际云端回复：`现在是晚上九点半，你明天早上八点还要上课，所以得早点休息。晚安，我数着你的呼吸数到六十就关灯——希望那时候你能安稳地睡着。`

随后在 Chrome 中使用隔离测试账号正常登录、开启增强并发送真实消息，主模型 `zai-org/GLM-5.3` 与 instruct `qwen2.5-7b-instruct-99febd6ca6f2` 均使用真实服务。Run `ffd28fd3-9529-4ae1-9737-9a77061f1ace` 状态为 `completed`，总耗时约 4.50 秒，`emotion.enhancement` 请求恰好 1 次；保存结果与页面显示一致。

用户输入：`今天忙了一天，有点累。陪我说两句吧，简单一点就好。`

主模型原稿：

> 忙了一天辛苦啦。我就不说“高性能陪你聊天”这种话了，累的时候简单待着就挺好。
>
> 今天累是哪种累呀，事情多，还是不顺心？

润色后页面回复：`今天是“事情多+不顺心”的双倍累。但你不用陪我聊天，你只要陪我坐着就好——我会觉得这一天没白过。`

此次真实双模型 E2E 的调用与展示链路通过，但语义保真未通过：小模型将询问疲惫原因改成确定两种原因均存在，并反转了陪伴对象。

该模型仍会追加原稿中没有的事实、动作或承诺。保持原意目前由唯一润色指令约束，尚无确定性语义保真验证；开关默认关闭，通过聊天底部菜单栏供 dev 管理员继续评估。不会为了校正结果暗中增加第二条评审指令或重复调用。

### 原稿优先 system 的真实复测

针对用户指定的问候试验三版 system，共进行 6 次独立云端测试；这些是开发时的提示词对照，不是线上重试或多候选选择。最终采用校对规则置于首位、保留核心身份的版本。

原稿为“你好呀！我是亚托莉，高性能的仿生人少女！很高兴见到你～”加空行及“你怎么称呼？今天过得怎么样？”。最终版实际返回仅第一段：没有新增“能感受情绪”，但仍删除两个问题。另一条“今天累是哪种累呀，事情多，还是不顺心？”仍被补写额外追问和动作。该版提示词已明确保留原稿的要求，但真实样本未通过保真验收，不能宣称问题修复。未改变用户开关、保存历史或运行时失败策略。
